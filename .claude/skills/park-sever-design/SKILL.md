---
name: park-sever-design
description: >
  Зафиксированный дизайн-канон сайта «Парк Север» (park-sever.ru). ОБЯЗАТЕЛЬНО подключай
  этот навык всякий раз, когда работаешь над любым визуалом этого проекта: вёрстка или
  правка страниц/блоков/секций, CSS/стили, дизайн-система, типографика, цвет, отступы,
  компоненты, галереи/слайдеры, hero, кнопки, карточки — даже если слово «дизайн» не
  прозвучало. Дизайн для заказчика критичен и зафиксирован: сначала дизайн, потом всё
  остальное, и от канона мы НЕ отходим. Любое новое дизайн-решение записывается сюда.
---

# Дизайн-канон «Парк Север»

Это **единственный источник правды по дизайну**. Заказчик сказал прямо: «для меня критически важно, чтобы был охуенный дизайн; сначала прорабатываем дизайн, потом делаем всё остальное; я хочу, чтобы дизайн был зафиксирован, и мы никогда больше от него не уходили».

Поэтому:
- **Дизайн — первичен.** Прежде чем писать логику/контент/интеграции — сначала довести визуал блока до уровня канона.
- **Канон фиксирован.** Не «улучшать на свой вкус» в обход правил. Если решение не покрыто каноном — спросить заказчика, принять решение и **дописать его в этот файл** (раздел «Журнал решений»).
- **Не отходим.** Любая правка должна проходить чек-лист в конце.

## Дух (что мы вообще делаем)

Загородный парк со стеклянными домами: «отдых для двоих среди леса». Настроение — **тишина, уединение, благородство, тёплый премиум**. Дорого, но не кричаще. Редакционная подача (как разворот хорошего журнала), много воздуха, крупные фото природы. Не «продающий лендинг с кучей плашек», а спокойное, уверенное пространство.

Антинастроение: дёшево, шумно, стоково, «инфобизнес», пёстро, тесно.

## Шрифты (ровно два)

- **Monotype Corsiva** (`--font-display`) — заказной курсивный скрипт. Только **заголовки и акценты** (h1–h3, hero-title, акцентные строки). Размер крупный, трекинг 0 (глифы связные). Это «голос бренда» — им не злоупотреблять в мелочах.
- **Lora** (`--font-head` / `--font-body`) — **весь остальной текст**: наборный (абзацы, лиды, подписи) и UI (кнопки, меню, eyebrow, числа). Веса 400/500/600.

Никаких третьих шрифтов. Sans-serif в макет не вводим.

## Цвет

Токены объявлены в `src/styles/design-system.css` (`:root`). Использовать **только их**, не хардкодить произвольные hex.

**ОСНОВНАЯ ПАЛИТРА САЙТА (заказчик 2026-07-04) — ровно эти цвета:**
1. **Тёмно-синий** `--navy #0F2A47` — главный структурный цвет (преобладает).
2. **Светло-бежевый** (парчмент, «как в Благородном севере») — светлая база: `--cream #f3f1eb` / `--sand #FAF2DA`.
3. **Зелёный** `--green #5F7D63` — как уже применён на главной («Наш мир»).
4. **Золотистый** `--gold-l #d3b07a` — **изредка, без фанатизма, малыми дозами** (акцент-слово, кнопка-бронь, тонкие линии). Не заливать им большие площади.

**Коричневый — СНЯТ из палитры.** Не используем как цвет сцен/фонов. Единственное исключение: одна тёмная «визитка» на главной (блок «Оазис») — перекрашена в тёмно-таупе `--bark #4C4038` (не тёплый коричневый #573B38). Больше коричневого нигде.

**Поверхности (токены):**
- `--canvas #ffffff` — основной холст.
- `--cream / --paper #f3f1eb` — светло-бежевая база (парчмент).
- `--sand #FAF2DA` — тёплая светло-песочная сцена.
- `--bark #4C4038` — тёмно-таупе: ТОЛЬКО единственная тёмная «визитка» (Оазис). Коричневый #573B38 снят.
- `--green #5F7D63` — природный зелёный.
- `--ink #28262a` — почти-чёрная сцена / основной цвет текста.

**Акценты:**
- `--navy #0F2A47` — главный структурный акцент (кнопки, ссылки, числа).
- `--gold-l #d3b07a` — золото сайта, изредка/малыми дозами. (`--gold #C9A86A` — только тонкие линии/номера.)
- `--copper #A67C52` — медь: **не используем** (наследие, на выход).

**Линии:** `--fog`, `--silver`, `--hair-gold` (тонкая золотистая обводка).

Ритм сцен: **база — светлые** (canvas/cream/sand/green); тёмные (navy/один блок bark/ink) — точечные акценты. Глубина строится **контрастом поверхностей**, а не тенями.

**Градиенты поверх фото — мягкие.** Оверлей нужен только чтобы читался текст; фото НЕ должно терять красоту. Держим пик альфы низким и не заливаем сплошным цветом больше нижней ~10% кадра — дальше быстрый спад в 0. Не «закрашивать» фото градиентом (заказчик 2026-07-04).

## Железные правила (от них не отходим никогда)

1. **Текст — всегда сплошной цвет.** На светлом фоне — `--ink` (#28262a). На тёмном фоне — **чисто белый `#fff`**. Никаких приглушённых «почти-белых» (`#ece5db`, `#cfccc6`, `#dbe2ea`…), никакого серого графита для наборного текста. Заказчик считает блёклый текст признаком «дешёвого/недоделанного».
2. **`opacity` для приглушения текста/цвета — ЗАПРЕЩЁН.** Никаких `rgba(255,255,255,.9)` на тексте. Если нужно «потише» — берём другой **сплошной** цвет. (Исключение: декоративные слои вроде зерна-текстуры, где альфа запечена в сам ассет, а не задана CSS-свойством `opacity`.)
3. **Кнопки — прямые углы** (`--r-btn:0`). Без капсул.
4. **Теней нет.** Глубина — контраст поверхностей + тонкая линия (`--hair-gold`/`--fog`). `box-shadow`/`text-shadow` не вводим.
5. **Много воздуха.** Щедрые вертикальные отступы секций, не лепим элементы вплотную. Контейнер `--wrap:1200px`.
6. **Фото — крупные, природные, тёплые.** Не мелкие иконки-стоки. Кадры — лес, дом, животные, свет «золотого часа».

## Анти-паттерны (сразу выглядит дёшево — не делаем)

- Серый/полупрозрачный текст; текст через `opacity`.
- Скруглённые «капсульные» кнопки, тени-подложки, градиентные кнопки.
- Пёстрая палитра, случайные hex мимо токенов.
- Тесная сетка без воздуха, мелкие стоковые картинки.
- Третий шрифт, sans-serif, разнобой кеглей.
- «Плашка на плашке», избыток рамок и бейджей.

## Текущие компоненты-эталоны

- **Hero** — сезонное видео на фон, тёмный scrim, крупный курсивный `hero-title`, инлайн-форма брони znms.
- **Блок «Оазис»** (`#oasis`, `sec--bark`) — ровная тёмно-коричневая сцена + горизонтальная карусель «фотокарточек» в белом паспарту **без рамки**, прямые углы; правый край карусели full-bleed до края экрана с мягким градиентом в `--bark`; без стрелок/точек, скролл + drag, следующая карточка выглядывает.
- Кнопки: `.btn--navy` (основная), `.btn--outline-light` (на тёмном).

## Процесс (как применять канон)

1. Прежде чем верстать блок — держи в голове дух и железные правила.
2. Сначала доводишь **визуал**, потом контент/логику.
3. Если упёрся в решение, которого нет в каноне (новый тип блока, новый приём) — предложи заказчику варианты, зафиксируй выбор и **допиши в «Журнал решений»** ниже. Канон должен расти, а не размываться.
4. Перед тем как сказать «готово», пройди чек-лист.

## Чек-лист перед «готово»

- [ ] Весь текст — сплошной цвет (светлый фон → ink, тёмный → #fff)? Нет серого/полупрозрачного?
- [ ] Нет `opacity` на тексте/цвете? Нет теней (`box-shadow`/`text-shadow`)?
- [ ] Кнопки с прямыми углами?
- [ ] Только два шрифта (Corsiva для заголовков/акцента, Lora для остального)?
- [ ] Цвета — из токенов `:root`, не произвольные hex?
- [ ] Достаточно воздуха, не тесно?
- [ ] Соответствует духу (благородно, тихо, премиум), не выглядит дёшево/стоково?

## Журнал решений

Сюда дописываем каждое новое зафиксированное дизайн-решение (с датой), чтобы канон рос и был полным.

- **2026-06-26** — Базовый канон зафиксирован: 2 шрифта (Corsiva + Lora), палитра-токены, синий `--navy` как главный акцент, текст сплошной (на тёмном #fff), прямые углы, теней нет, много воздуха. Блок «Оазис» — тёмная сцена + зерно + карусель-«фотокарточки» без стрелок/точек.
- **2026-06-26** — **Единая левая линия.** Весь контент выравнивается по одной левой линии — как hero и навбар: контейнер прижат к левому краю экрана, текст стартует в **32px** от края. Реализация — класс `.wrap--flush` (`max-width:none; margin-left:0`) поверх `.wrap`. Не центруем блоки по 1200px (от этого на широких экранах левый край «уезжает» вправо и ломает линию). Узкую колонку текста задаём через `max-width` на самом текстовом блоке, а не центровкой контейнера.
- **2026-06-26** — **Наборный абзац (lead) = Lora 20px / line-height 1.4 / вес 500.** Это эталонный размер основного текста (как лид в hero). Класс `.body` по умолчанию мельче (16px/1.6) — для крупных «редакционных» абзацев поднимаем до 20/1.4 (см. абзацы блока «Оазис»). Цвет — сплошной (тёмный фон → #fff).
- **2026-06-26** — **Левую линию даём двумя способами:** `.wrap--flush` (full-bleed, `max-width:none`) — для каруселей/галерей, которые должны доходить до края экрана; `.wrap--left` (`margin-left:0`, ширина остаётся 1200px) — для обычных контентных блоков, прижатых влево. Старт обоих — 32px от края.
- **2026-06-26** — **Бирки (eyebrow / надписи-ярлычки над заголовком) НЕ используем.** Заказчик их не любит. Сняты со всех блоков. Над заголовком сразу заголовок, без мелкого ярлычка-надстрочника. Класс `.eyebrow`/`.label-eyebrow` в новой вёрстке не вводим.
- **2026-06-26** — **Карусель «Оазиса» доработана:** зерно убрано (на экране не читалось) — фон ровный `--bark`; «фотокарточки» — чистое белое паспарту **без рамки**; правый край карусели — мягкий линейный градиент в цвет сцены (`rgba(46,34,24,0)→--bark`), фото «уходят» под край. Карточки и фото — **прямые углы** (скругление убрано).
- **2026-06-26** — **Тёмные «нарядные» блоки (Оазис, Искусство отдыха) — фон `--bark`, заголовок белый.** От золота в заголовках на этих блоках отказались (золото остаётся только как тонкий металл/линии при необходимости). Фото в карточках — без скругления, теней нет (убран `text-shadow` у номеров).
- **2026-06-26** — **Блок 3 «Искусство отдыха» — карточки без рамок, текст на фото.** Единая карточка = фото на всю ширину карточки + текст (номер белым крупно, заголовок, описание) поверх фото у нижнего края на **линейном коричневом градиенте** (`--bark`, плотный снизу → прозрачный кверху). Блок во всю ширину (`wrap--flush`), шапка по центру, кадр ~8:9.
- **2026-06-26** — **Блок 4 «Дом среди леса» — полноэкранный фото-слайдер с текстом поверх.** Кадры во всю ширину/высоту (~82vh), текст (заголовок Corsiva, абзацы 20/1.4, кнопка navy) слева на линии 32px поверх фото на коричневом градиенте `--bark`. Механика слайдера прежняя (стрелки/точки — внизу справа).
- **2026-06-26** — **Единый коричневый `--bark = #573B38`** (был #2E2218 + промежуточные `--bark-2`/`--bark-warm` — свёрнуты в один). Все тёмные сцены и градиенты поверх фото в блоках 2–4 — на нём.
- **2026-06-26** — **Палитра сводится к 3 цветам + чёрный/белый.** Коричневый `--bark #573B38`, синий `--navy #0F2A47`, зелёный `--forest #385751` (добавлен в токены, но **пока НЕ используется** — резерв). Базовые — чёрный текст `--ink` и белый. Золото/медь — на выход (остаются максимум как тонкий металл, не как заметный акцент).
- **2026-06-27** — **Иконки — Material Symbols (Google), залитые (FILL=1).** Концептуальные иконки берём из Material Symbols, начертание filled (сплошной силуэт), а не штриховое. Где у Material даже «fill» рисуется линиями (подарок: `redeem`, `featured_seasonal_and_gifts`) — берём сплошной силуэт из другого набора (подарок → Heroicons solid `gift`). Вставляем инлайн-SVG, `fill="currentColor"` (цвет из CSS), `width/height 100%` (размер задаёт контейнер). Блок «Атмосфера для двоих»: иконки крупные (104px), цвет `--copper`, подписи и заголовок — белые; раскладка иконок — flex по центру с зазором (не растянутая сетка).
- **2026-06-27** — **Блок 5 «Атмосфера для двоих» — галерея как в «Оазисе».** «Фотокарточки» в белом паспарту, прямые углы, карусель со скроллом/драгом, следующая выглядывает. Вынесена в отдельный `wrap--flush`: левая карточка от линии 32px, ряд тянется во всю ширину, правый край обрезается экраном. Карточки **мельче** оазисных (`.oasis-card--sm`: `flex:0 0 clamp(200px,26%,300px)`). Градиент правого края — в цвет navy-сцены (`rgba(15,42,71,0)→--navy`).
- **2026-06-27** — **Блок 6 «Наш мир» — редакционная мозаика, без полупрозрачного градиента.** Одно крупное «геройское» фото слева (`.world-tile--hero`, aspect 5/6) + 2×2 мелких справа (`.world-mosaic__side`, aspect 4/5). Подписи — серифом (Lora 600) **ПОД** фото, сплошной `--ink` (не белым поверх фото на rgba-градиенте — старый оверлей убран как нарушение канона). Фото — прямые углы, hover — лёгкий zoom 1.04. На узких экранах мозаика схлопывается в одну колонку.
- **2026-07-01** — **Больше светлого фона; коричневый — редкий акцент.** Заказчик: «светлого нужно больше, коричневый — изредка, убираем его из основного». Ритм сцен меняется: **база — светлые поверхности** (`--canvas`/`--cream`/`--sand`/`--gold-l`); тёмные сцены (`--bark`/`--navy`) — точечные акценты, а не половина страницы. Коричневый `--bark` — максимум **один** заметный блок на странице (сейчас — «Оазис»), не два подряд.
- **2026-07-01** — **«Отдых» = синий.** Блок «Искусство отдыха» (`#art-of-rest`) переведён с коричневого на `sec--navy`; градиент оверлея карточек (`.art-step__overlay`) — тоже navy (`rgba(15,42,71,…)`). Тема «отдых / искусство отдыха» закрепляется за синим.
- **2026-07-01** — **Блок 2 «Оазис» переверстан как «карточка-визитка о нас».** Блок намеренно выбивается из общего ритма страницы (шрифты/цвета — каноничные, раскладка — своя). Фон сцены — коричневый `--bark` (это *единственный* заметный коричневый блок). Внутри — двухколоночная раскладка `.oasis-layout` (grid `minmax(320px,46%) 1fr`, gap 44px): **слева** светлая бумажная плашка `.oasis-paper` (фон `--cream`, текст тёмный `--ink` — снимает мрачность коричневого), **справа** горизонтальная карусель. Карусельные «фотокарточки» — паспарту **вдвое шире** (`padding:16px`, было 8px) и **в цвет светлой бумаги `--cream`** (не белое), т.к. фон блока тёмный (`.oasis-card--paper`, `.oasis-carousel--wide`). Строка-«чипсы» под текстом («12,4 га • Без соседей • …») — курсивным скриптом **Monotype Corsiva** (`--font-display`, 27px, цвет `--bark`), а не Lora. На узких экранах (≤900px) колонки схлопываются в одну. Правило на будущее: **дальше по странице фото на прокрутке — мелкие**, крупных кадров избегаем (шумно).
- **2026-07-01** — **«Оазис»: бумажная плашка = вырезанный лист бумаги, фон блока — коричневый (дерево отменено).** Идею текстуры дерева под всей секцией **отменили** — фон остаётся сплошной `--bark` (как был). Левая плашка `.oasis-paper` получает фоном **вырезанный лист бумаги** (`src/assets/images/paper.png` — парчмент с прозрачным «рваным» краем, оптимизируется в webp через `getImage`, URL кладётся инлайном в `background-image`; `background-size:cover`, `--cream` — фолбэк). Лист «лежит» на коричневом. `public/images/textures/wood.jpg` пока не используется (резерв). Тень под бумагой — **пока НЕ добавлена** (ждём решения по исключению из правила «теней нет»).
- **2026-07-01** — **Золото `#d3b07a` (`--gold-l`) в hero — по эталон-макету заказчицы.** Куда кладём золото в hero: (1) **акцент-слово заголовка** («Север») — `.hero .hl{ color:var(--gold-l) }` (заголовок разбивается в фронтматтере на `heroTitleMain` + последнее слово `heroTitleAccent`); глобальный `.hl` вне hero остаётся navy. (2) **Курсивная строка-подзаголовок** `.hero-accent` («Отдых для двоих среди леса») — золотая. (3) **Кнопка «Забронировать» в шапке** `.nav__book` — золотой фон, текст тёмный `--ink`. Единый золотой CTA-класс `.btn--gold` тоже переведён на `--gold-l` (текст `--ink`). `--gold-l` — это ЕДИНОЕ «золото» сайта (старый `--gold #C9A86A` остаётся только для тонких линий/номеров).
- **2026-07-01** — **Светлый золотой `--gold-l:#d3b07a` РАЗРЕШЁН на фон.** Тёплый светло-золотой (эталон — цвет кнопки «Забронировать» в hero: берём только ЦВЕТ, не стиль кнопки). Единственное золото, которое можно класть сплошной заливкой на фон сцен/плашек. Класс поверхности — `.sec--gold` (фон `--gold-l`, текст тёмный `--ink`, т.к. фон светлый). Акцентное золото на тёмном (`--gold #C9A86A`) остаётся для тонких линий/номеров — не путать.
- **2026-07-01** — **Все контентные блоки главной прижаты к левой линии 32px (`wrap--flush`), включая «Новости/Отзывы» и подвал.** Заказчик: никакого «сильного отступа»/центровки — левый край всего контента стоит на единой линии 32px, как hero/навбар. Перевёл на `.wrap wrap--flush` блок 8 «Истории Парка» (`#reviews`) и `footer.foot`. Правило: **по умолчанию контентный `.wrap` на главной — `wrap--flush`** (или `wrap--left`, если full-bleed не нужен), голый центрированный `.wrap` на главной не используем.
- **2026-07-01** — **Блок 5 «Атмосфера для двоих» — финальная доводка.** (1) **Заголовок блока — золотой `--gold-l`** (`#atmosphere .h2{ color:var(--gold-l) }`), перебивает белый заголовок navy-сцены. (2) **Иконки — тем же основным золотом `--gold-l`** (было `--copper`), остаются «дышащими» (анимация `icon-breathe`), но **мелкие: 52px** (было 140px). (3) **Одинаковое расстояние между иконками:** колонке иконки задана **фиксированная ширина 132px** (`.atmosphere-icon{ flex:0 0 132px }`, `gap:56px`) — длинные подписи («Предложение руки и сердца») **переносятся на 2 строки** (`line-height:1.25`), не раздвигая соседние иконки. (4) **Весь блок в один экран:** `#atmosphere{ min-height:100vh; display:flex; flex-direction:column; justify-content:center }`, отступы поджаты. (5) **Фото карусели ниже:** `.oasis-card--sm{ flex:0 0 clamp(220px,30%,340px) }`, кадр `aspect-ratio:4/5` (было 3/4) — карточки мельче и ниже, блок влезает.
- **2026-07-01** — **Блок 6 «Наш мир» — ОДИН ряд из 5 вертикальных (портретных) карточек.** Итог: двухрядная мозаика при вписывании в один экран давала «летербокс» — карточки широкие, но узкие по высоте, фото не читались (заказчик забраковал). Решение: `#our-world .world-mosaic{ grid-template-columns:repeat(5,1fr); grid-auto-rows:60vh; gap:20px }`, все плитки `grid-column:span 1` (спаны `--wide`/`--med` перебиты), `.world-tile__pic{ aspect-ratio:auto; height:100% }`. Секция `min-height:100vh; justify-content:center` центрирует ряд; один ряд 60vh + шапка + кнопка помещаются в экран, фото **высокие, всё видно**. Адаптив: ≤900px — 2 колонки (`42vh`, `min-height:0`), ≤560px — 1 колонка (`46vh`). ВАЖНО: **не растягивать мозаику через `flex:1`** (раздувает карточки) и **не делать два ряда «летербоксом»** (узко по высоте) — держим один ряд портретных кадров.
- **2026-07-01** — **Блок 8 «Истории/Отзывы» — подзаголовки колонок крупнее и с подчёркиванием.** «Наши истории» и «Отзывы» были незаметны: `.stories-col h3{ font-size:clamp(26px,3vw,34px); padding-bottom:10px; border-bottom:2px solid var(--navy) }` — крупный серифный заголовок с тонкой синей линией снизу.
- **2026-07-01** — **Блок «Как добраться» (`#contacts`) — левая часть = адрес + «Построить маршрут».** Вместо строки «Мы всегда на связи…»: **адрес** «РФ, Московская область, Дмитровский м.о., дер. Василево.» + телефон + кнопки. Главная CTA — `.btn--navy` **«Построить маршрут»** на deep-link Яндекс.Навигатора (точка дер. Василёво, `yandex.ru/navi?whatshere…`), рядом WhatsApp.
- **2026-07-01** — **Блоки «в один экран» = ОДИН ряд ВЕРТИКАЛЬНЫХ (портретных) карточек, высота ряда фикс. `vh`.** Итоговое правило для блоков 3 «Искусство отдыха» и 6 «Наш мир». Заказчик прямо забраковал два варианта: (а) `flex:1`-растягивание рядов (раздувает кадры) и (б) широкие «летербокс»-карточки (низкие по высоте — «узкие, ничего не видно»). Правильно — **вертикальные кадры в один ряд**: секция `min-height:100vh; display:flex; flex-direction:column; justify-content:center`; сетка `grid-template-columns:repeat(N,1fr); grid-auto-rows:<vh>` (блок 6 — 5 карточек × `60vh`; блок 3 — 6 карточек × `58vh`); плитки `.…__img/…__pic{ height:100%; aspect-ratio:auto }`. Так кадры высокие/портретные, всё видно, блок влезает в экран. Адаптив: ниже ~1100px `min-height:0` (отпускаем «один экран»), сетка сворачивается в 3→2→1 колонку, кадр `aspect-ratio:3/4`, обычная прокрутка. Прежний flex-fill в блоке 3 снят.
- **2026-07-04** — **Финальная палитра + мягкие градиенты (глобально, все страницы).** Заказчик: «с дизайном мы на финале». (1) **Основная палитра сайта — 4 цвета:** тёмно-синий `--navy`, светло-бежевый `--cream/--sand` («как в Благородном севере»), зелёный `--green` (уже на главной), золотистый `--gold-l` — изредка, малыми дозами. (2) **Коричневый снят.** Тёплый коричневый `--bark #573B38` → перекрашен в тёмно-таупе **`#4C4038`** (нейтральнее, без «рыжины»); остаётся ТОЛЬКО на одной тёмной «визитке» — блок «Оазис» на главной. Больше коричневого фона нигде. Хардкод-фейды карусели «Оазиса» переведены с `rgba(87,59,56…)` на `rgba(76,64,56…)`. (3) **Все градиенты поверх фото смягчены** (`hero__scrim`, `house__scrim`, `art-step__overlay`, `world`-подписи, `forwhom__scrim`, `finale__scrim`, `detail-card__cap`): понижен пик альфы, сплошная заливка — только узкая полоса у нижнего/левого края под текст, дальше быстрый спад в 0. Цель — фото не теряет красоту, оверлей лишь обеспечивает читаемость текста.
- **2026-07-01** — **Вкладка «Дом» (`/accommodation/house-1`) сверстана.** Ритм: светлые сцены — база, две тёмные фото-банды (для-кого, финал) — точечные акценты. Блоки: (1) первый экран — текст слева + карусель-«фотокарточки» на синей сцене `sec--navy` (белые паспарту, правый градиент в `--navy`, CTA `.btn--gold`); (2) «Для кого» — короткий блок на приглушённом фото (сплошной тёмный градиент, не opacity), белый текст, список с золотыми штрихами; (3) «Дом, который хочется рассматривать» — крупные карточки, подписи внизу, слайд-шоу кадров (жёсткая смена через `visibility`); (4) фильмстрип на всю ширину + лайтбокс; (5) «Продумано до мелочей» — чек-лист + обычная карусель + кнопка «Полная комплектация» → боковая шторка-модал; (6) «Отдых начинается до приезда» — фото слева/текст справа; (7) финал — фото под градиентом, курсив по центру, иконки-настроения, CTA `.btn--gold`. Фото оптимизируются заранее (`scripts/optimize-house.mjs`, sharp → max 2000px) в `src/assets/images/house/`. Full-bleed-секции (карусель, фильмстрип) обязательно с `overflow:hidden` — иначе `margin-right:-32px` даёт горизонтальный скролл всей страницы. На странице — рантайм-типограф (висячие предлоги → неразрывный пробел).
- **2026-07-01** — **Вкладка «Дом», блок «Дом, который хочется рассматривать» — карточки переведены на канон-приём «текст на фото поверх градиента» и уменьшены.** Заказчик: «сделай карточки меньше, возьми наш стиль с главной — как обрамляем карточки, градиент снизу». Было: 2 крупные карточки-колонки, фото 4/3, подпись тёмным текстом ПОД фото. Стало (`.detail-grid`/`.detail-card`): один ряд компактных **портретных** кадров `grid-template-columns:repeat(4,1fr); gap:20px`, `.detail-card__media{ aspect-ratio:3/4 }`; подпись лежит НА фото у нижнего края на **сплошном navy-градиенте** (`.detail-card__cap{ position:absolute; bottom:0; background:linear-gradient(to top,var(--navy) 78%,rgba(15,42,71,0)) }`), текст белый (заголовок 18px, описание 14px). Тот же приём, что `.art-step` на главной, — единый card-язык сайта. Длинные подписи в такой карточке не помещаются → подписи держим короткими (подрезал текст карточки «Комфорт без лишних действий» до одной фразы); страховка от переполнения — `-webkit-line-clamp:4` на `.detail-card__text` (без opacity). «Скоро»-плейсхолдер остаётся пустой рамкой `--hair-gold` под тем же градиентом. Адаптив: ≤900px — 2 колонки, ≤520px — 1.
- **2026-07-01** — **Вкладка «Дом»: секция «Загляните внутрь» (фильмстрип) — фон с `--cream` на `--canvas` (белый).** Заказчик: «убери эту полоску, цвет из полоски» — кремовая секция между белыми блоками читалась как лишняя цветная полоса. `.filmstrip-sec{ background:var(--canvas) }`. (Правило шире: цветные банды среди светлых блоков не плодим без нужды — только осознанный ритм сцен.)
- **2026-07-01** — **Вкладка «Дом»: фото карусели «Продумано до мелочей» — в наш стиль (чистый кадр), убрана золотая обводка + кремовое паспарту.** Заказчик: «фотография совершенно не в нашей обводке, не в нашем стиле». Базовый `.slider-snap__slide` даёт паспарту `--paper` + рамку `--hair-gold` (старый вариант «Оазиса»); для этого слайдера перебито: `.kit__slider .slider-snap__slide{ background:none; padding:0; border:0 }` — фото во весь слайд, прямые углы, без рамки. **Канон: фото = чистый кадр без золотой обводки/паспарту** (паспарту оставляем только там, где сцена тёмная и это осознанное решение, напр. карусель «Оазиса»).
- **2026-07-01** — **Блок 3 «Искусство отдыха»: фон `#EEF4F8`, весь блок в один экран, градиент на фото оставлен.** По итогу выбора заказчицы (рассмотрела ~20 вариантов): (1) **Фон сцены — холодный светло-голубой `--frost:#EEF4F8`** (новый токен), класс `.sec--art` (текст тёмный `--ink`, заголовок чёрный). Заменил `sec--navy`. (2) **Весь блок умещается в один экран без прокрутки** — заказчица хочет видеть блок целиком. Реализация: `.sec--art{ min-height:100vh; display:flex; flex-direction:column; justify-content:center }`, `.wrap` — `flex:1` колонка, `.art-steps` — `flex:1; min-height:0; grid-auto-rows:1fr` (два ряда 3×2 делят высоту поровну), фото `.art-step__img{ height:100%; flex:1 }` без `aspect-ratio` — тянутся по доступной высоте (**крупные, но без скролла**). (3) **Градиент текста на фото ОСТАВЛЕН** (не вернулись к плашкам): аргумент — градиент лежит НА фото, а не на фоне страницы, поэтому светлый фон ему не мешает; оверлей остаётся navy (тема «отдых = синий»), текст белый. Заказчица допускала возврат к плашкам, если градиент на светлом некрасив — решение можно пересмотреть, если не понравится вживую. На узких экранах (≤860px) «один экран» отпускаем: `min-height:0`, фото возвращают портрет `aspect-ratio:8/9`, блок прокручивается.
- **2026-07-06** — **Новая вкладка «Новости» (`/news/`) — лента ВК журнальным разворотом; на главной блок «Истории Парка» остаётся компактным.** По решению заказчика красивая лента живёт на отдельной странице, а главная — как была (узкая колонка «Наши истории» + «Отзывы» в `stories-split`). Страница `/news/`: шапка-строка `.news-head` (h1 «Наши новости» слева + «Мы ВКонтакте →» navy справа, по базовой линии), далее `.news-spread` — grid `minmax(0,1.1fr) minmax(0,1fr)`, gap 56px: **слева лид** (свежий пост крупно: чистый кадр 16/10, дата navy 13/600, заголовок Lora 700 clamp(22–28), текст 17/1.5 с клампом 3 строки, «Читать в ВКонтакте →» navy), **справа колонка из трёх компактных постов** `.news-item` (миниатюра 168px 4/3, разделены тонкой линией `--fog`, кламп 2 строки). Посты после четвёртого — ряд карточек `.news-grid` (3 колонки, фото 4/3, подпись под фото). Низ ленты — `.news-cta`: курсивная строка Corsiva «Больше историй — в нашей группе» (navy) + кнопка `.btn--navy` «Открыть ВКонтакте». Заголовки карточек — **Lora** (тексты из ВК произвольные, Corsiva не для них). Фон светлый (canvas) → текст сплошной `--ink`, даты/ссылки navy; фото — чистые кадры, прямые углы, hover-zoom 1.04, теней нет. Адаптив: ≤900px разворот в одну колонку и сетка в 2, ≤560px сетка в 1, ≤520px миниатюры 120px. В меню добавлена вкладка **«Новости»** (после «Наш мир», перед «Историями»). Данные: общий модуль `src/lib/vk-news.ts` (снапшот `public/vk-news.json` → seed-фолбэк; парсер `scripts/fetch-vk-posts.js`, отдаёт 9 постов); к заголовкам/текстам на билде применяется типограф (висячие предлоги 1–2 буквы клеятся NBSP).
- **2026-07-06** — **«Искусство отдыха»: доводка после замечания заказчика («пространство справа, обрывисто»).** (1) **`wrap--left` на странице заменён на `wrap--flush` везде** — контейнер, обрезанный 1200px и прижатый влево, на широких экранах оставлял большую пустоту справа; правило подтверждено: контентный `.wrap` по умолчанию — `wrap--flush`, узкую колонку даёт `max-width` на самом тексте (`.aor-p` 62ch). (2) **Единый кегль наборного:** inline-переопределения font-size (16/17/18px) сняты; наборный абзац — `.aor-p` 20/1.4, второстепенная заметка — единственный младший класс `.aor-p--sm` 16/1.55. Текст плашек `.aor-plate__x` — 16px. (3) **Ритм сцен упрощён** (было пёстро: sand + frost + green + navy): база — чередование canvas/cream (этапы 1–2, 4–6), одна тёмная сцена navy («Ночь вдвоём»), frost — только «Сколько времени нужно», зелёные полосы-выводы — повторяющийся мотив после введения и этапа 2, финал navy. Правило: **на одной странице не плодить больше 2–3 цветных сцен поверх базовой пары canvas/cream.**
- **2026-07-06** — **«Искусство отдыха»: единый карточный язык, декоративные разделительные линии сняты.** Заказчик: «много стилей, всё перемешано, нет единой логики, много лишних разделительных линий». Было 6 типов линий (gold border-left у `.aor-callout`/`.aor-intro-note`, navy 2px над `.aor-qa h3` и `.aor-tier`, hair-gold над `.aor-plate`/`.aor-ritual`/`.aor-helper__fact`, fog внутри ритуала, gold-l над пунктами `.aor-atm`) и 4 разных карточных стиля. Сведено к системе: **(1) поверхности — ровно две**: светлая карточка (cream на canvas / canvas на cream: `.aor-card`, `.aor-callout`, `.aor-plate`, `.aor-ritual`, `.aor-helper`, `.aor-tier`) и navy-карточка (`.aor-callout--navy`) как редкий акцент; **все борды/линии на карточках убраны** — глубина только контрастом поверхностей. **(2) Единственный линейный мотив — золотой штрих-маркер списка** (`.aor-list li::before`); пункты «Атмосфера» (`.aor-atm`) переведены на тот же штрих (3 колонки, 17px), а не линию сверху. **(3)** Колонки «вопрос-ответ» — чистые текстовые (без синей линии над h3); заметка введения — обычный наборный 20/1.4 без золотой линии слева. **(4)** Инлайн-кегли `.aor-line` заменены модификатором `.aor-line--sm` (clamp 22–28). «Плашка-на-плашке» снята: `s1.social` — обычный абзац, не callout. Правило: **в лонгридах не вводить больше двух карточных поверхностей и не использовать линии как декор — линия допустима только как штрих-маркер списка.**
- **2026-07-06** — **Верхняя карусель «Наш мир» = Blob Carousel из ТЗ (codepen.io/pehaa/pen/QYeqBO, Paulina Hetman) — прямое требование заказчика, перебивает «прямые углы» для этого блока.** Фото 1.1–1.6 в органических «каплях»: svg clip-path (7 форм из пена) с морфингом формы через нативный SVG `<animate attributeName="d">` (anime.js не подключаем); бесконечная лента `translate3d(-50%)` 38s + клонирование элементов реверсом в начало (JS из пена); параллакс-глубина слоёв — `perspective:.5px` на обёртке + `translateZ/scale` на элементах (`--special…`). Классы `.ow-blob__*`. Первый вариант (прямоугольная центр-карусель «по канону») заказчик отклонил: референс из ТЗ обязателен к точному переносу. Правило на будущее: **если в ТЗ дана конкретная ссылка-референс на приём/анимацию — переносим именно её**, канон адаптирует только цвет/типографику вокруг, не подменяет механику и форму.
- **2026-07-07** — **Вкладка «Атмосфера для двоих» (`/atmosphere/`) сверстана по ТЗ «ч.2 04.07» + встроенному в докс макету.** Тексты — только из `src/data/atmosphere.json` (генерится `scripts/extract-atmosphere.mjs`: дословный программный перенос из docx + транскрипция макета; руками не перепечатываем). Фото — `src/assets/images/atmosphere/` (`scripts/optimize-atmosphere.mjs`). Ритм сцен: тёмный hero (кадр 13.4, мягкий скрим) → canvas «Для вашей истории» (4 карточки-повода cream + строка из 7 иконок, золото `--gold-l` 44px, подписи ink) → cream «Атмосфера сезона» → зелёная сцена «Отдых начинается ещё до вашего приезда» → canvas «Примеры работ» → cream финал с формой. Приёмы: **сезонные карточки — 8 шт. двумя рядами по 4, второй ряд со смещением вправо (`.season-row--offset{ padding-left:56px }`)** — как просило ТЗ; клик по карточке открывает лайтбокс-галерею её фото («Внутри Фото N.1–N.x»); внутри карточки праздник = Lora 700, заголовок настроения = Corsiva 24 navy, теги внизу navy через «•». «Примеры работ» — слайдер фото + **текст только по нажатию** (панель cream, список с золотым штрихом, итого navy 700) — прямое требование ТЗ. Блок «до приезда» — слайдер 13.1–13.4 со стрелками и точками (по ТЗ), список доп-услуг с золотым штрихом, акцентная фраза «Вам остаётся только приехать…» — Corsiva белым (по ТЗ «курсивом»). Форма «Рассказать о своей истории» — поля раскрываются по кнопке (по ТЗ), отправка пока `mailto:` на park-sever@inbox.ru (бэкенда нет — заменить на форм-сервис, когда решим); поля — белые, прямые углы, рамка `--fog`. Баннер «Сейчас в Парк Север» с макета ч.1 НЕ перенесён (в ч.2 не упомянут, дословный текст есть только весенний) — ждём решения заказчика.
- **2026-07-08** — **Гиперссылки в контенте — ЧЁРНЫЕ, в цвет текста, с подчёркиванием.** Заказчик явно: «смотреть фото — чёрный цвет». Текстовая ссылка на светлом фоне — `--ink` + underline (`text-underline-offset:3px`); ссылку выдаёт подчёркивание, а не цвет. На тёмных сценах — белая с подчёркиванием. База: безклассовые `<a>` внутри `.sec` получают это автоматически; «Смотреть фото» на сезонных карточках — ink с постоянным подчёркиванием. Применено ГЛОБАЛЬНО ко всем текстовым ссылкам и псевдоссылкам-кнопкам: «Смотреть фото», «Что было подготовлено»/«Свернуть» (.atm-ex__toggle), «Читать в ВКонтакте» (.news-more), «Мы ВКонтакте» (.news-head__vk), .news-card__more, .rcard__a, .link-accent/.link-gold — все ink + underline, hover снимает подчёркивание (у toggle). Navy для ссылок больше не используем; navy остаётся у не-ссылок (даты .news-date, .modal__meta) и структурных элементов (кнопки).
- **2026-07-08** — **«Атмосфера»: строка иконок «что можем подготовить» = бегущая лента-полоса (marquee).** Заказчик: «сделать как линию/полосу, по которой двигаются эти штуки». Реализация `.atm-marquee`: кремовая полоса на всю ширину, внутри трек из ДВУХ одинаковых групп иконок (`.atm-marquee__group`, вторая — `aria-hidden`), CSS-анимация `translate3d(-50%)` linear infinite ~42s → бесшовный цикл; движется ВСЕГДА (паузу на hover заказчик снял), `prefers-reduced-motion` — анимация отключается. Полоса — full-bleed до краёв экрана (`margin:24px -32px 0` поверх паддингов `.wrap` + `overflow:hidden`), БЕЗ фона — лента живёт прямо на холсте. Иконки золото `--gold-l` 44px, подписи ink — как были. SVG вынесены в фронтматтер массивом строк (`set:html`), чтобы ряд рендерился дважды без дублирования разметки.
- **2026-07-08** — **«Атмосфера», карточки-поводы «Для вашей истории» — светлые, иконка белая НА фото.** Заказчик: «цвет ближе к светлому; иконки прямо на фото, в левый верхний угол, белые». Карточка `.atm-card` — фон `--canvas` (кремовая плашка снята), тело без боковых отступов (текст под фото, как подписи «Наш мир»); иконка `.atm-card__icon` — белый залитый силуэт 30px, `position:absolute; top:14px; left:14px` поверх кадра, без подложек и теней.
- **2026-07-07** — **Контентные фото-слайдеры: СТРЕЛОК НЕТ, фото сменяются САМИ, внизу аккуратные точки — как блок «Дом среди леса» на главной.** Заказчик увидел кнопки-стрелки (`.slider-arrows`) в «Примерах работ»: «таких переключалок у нас нет, отвратительно» → эталон — слайдер главной: стрелки скрыты (`display:none`), автосмена кадров каждые 5с (`scrollTo` smooth, индекс считается от текущего `scrollLeft`), точки `.slider-dots` (8px, `--fog`, активная — акцент) синхронизируются со скроллом и кликабельны; свайп — нативный. Кадр — чистый (без паспарту/рамки): общий модификатор `.slider-snap__slide--clean`. Так работают: «Атмосфера» — карусель 13.x «до приезда» и фото «Примеров работ»; «Дом» — «Продумано до мелочей». Оазис-карусель (драг, карточки-паспарту, следующая выглядывает) остаётся отдельным приёмом для полноширинных лент, НЕ для смены кадров в одном окне. Вариант с кнопками-стрелками не возвращать никогда.
- **2026-07-07** — **Подпись под заголовком секции — вплотную к заголовку (~8px).** Заказчик: «текстовый блок значительно ближе к заголовку, пикселей 10». Глобально: `.shead{ gap:8px }` (было 18px), инлайн-`margin-top:8px` у подписей внутри `.shead` на главной сняты (не дублировать gap!), `.aor-head .aor-sub{ margin-top:6px }` (было 14px). Правило: связка «заголовок + подпись-гирка» всегда идёт плотно, воздух даём ПОСЛЕ связки (margin-bottom самого `.shead`), а не между заголовком и подписью.
- **2026-07-06** — **Вкладка «Новости» переименована в «Истории» и переехала на `/stories/`.** Весь контент ленты ВК (журнальный разворот, описан в записи выше) без изменений живёт теперь на странице `/stories/` (h1 «Наши истории», секция `#stories`); отдельной вкладки «Новости» в меню больше НЕТ. Пункт меню «Истории» на всех страницах ведёт на `/stories/` (раньше — якорь `/#reviews`). Блок «Истории Парка» (`#reviews`) на главной остаётся как компактный блок (истории + отзывы Яндекс), но из меню на него больше не ссылаемся.
- **2026-07-07** — **«Искусство отдыха», этап 1 «До поездки»: барьеры — плашками, «про женщин/про мужчин» — наборным текстом с иконками, фото дороги убрано.** По правке заказчика: (1) сразу после лида — плашки `.aor-plate` с барьерами («Чувство вины за отдых», «Страх потерять контроль», «Перфекционизм»…); (2) НИЖЕ них — два абзаца про женщин и мужчин в две колонки (`.aor-duo.aor-genders`) **обычным наборным `.aor-p` 20/1.4, БЕЗ плашек**, слева от абзаца — иконка `woman`/`man` (Material Icons filled, 34px, navy, класс `.aor-gender`); (3) фото дороги (art-step-1) из этапа удалено — лид идёт во всю ширину без `.aor-split`.
- **2026-07-08** — **«Атмосфера», карточки праздников «Атмосфера сезона»: заголовок — ОДНОЙ строкой, одним стилем.** Заказчик: «объединим заголовок в один стиль, один шрифт, один цвет — составной, через двоеточие». Было: праздник Lora 700 ink + отдельная строка настроения Corsiva 24 navy. Стало: один `h3.season-card__label` = «Праздник: настроение» (настроение со строчной после двоеточия, `toLowerCase()` в разметке), Lora 700 19/1.3, цвет `--ink`; класс `.season-card__title` удалён. Кадры карточек-поводов «Для вашей истории» — портретные `aspect-ratio:3/4` (было 4/3).

- **2026-07-08** — **«Атмосфера сезона»: ссылка «Смотреть фото» — В ПЛАШКЕ карточки (низ тела), БЕЗ стрелки.** Пробовали класть на фото с градиентом (navy и светлым) — заказчик откатил: подпись остаётся в белой плашке, navy 600 14px, hover — подчёркивание. Градиентов на кадре карточки НЕТ.
- **2026-07-08** — **«Атмосфера сезона»: строка тегов («Праздник за городом • Отдых зимой»…) УБРАНА из карточек.** В плашке остаются: заголовок-связка, текст и «Смотреть фото» (прижата к низу `margin-top:auto`). Класс `.season-card__tags` удалён; теги в atmosphere.json остаются лежать без вывода.
- **2026-07-08** — **Автослайдеры на одной странице листаются ВРАЗНОБОЙ, не хором.** Несколько `.slider-container` с одинаковым интервалом стартовали одновременно и переключались синхронно — выглядит механически. Правило: каждому слайдеру — свой стартовый сдвиг (`3500 + i*1400 мс`) и чуть разный интервал (`5000 + i*650 мс`), интервалы разные → со временем расходятся ещё сильнее. Применено на «Атмосфере» (карусель «до приезда» + 4 слайдера «Примеров работ»).
- **2026-07-08** — **Лайтбокс: стрелки-«коробки» по краям экрана СНЯТЫ — навигация лежит НА фото.** Заказчик: стрелки, отделённые от фотографии, «выглядят очень плохо». Новая схема (обе страницы — «Атмосфера» и «Дом»): кадр обёрнут в `.lightbox__stage` (position:relative вокруг img), кнопки — тонкие шевроны `‹ ›` (46px, белые, БЕЗ рамки/подложки) у левого/правого края самого кадра; кликабельна вся боковая зона фото (26% ширины, во всю высоту); hover — шеврон золотится `--gold-l`. Рамочные кнопки в лайтбоксе не возвращать.
- **2026-07-08** — **«Примеры работ»: раскрытие панели — плавное, стрелки у тоггла сняты.** Кнопка — просто «Что было подготовлено» / «Свернуть» (без → и ↑). Резкое display:none→block заказчик забраковал («в стиле 70-х»). Канон-приём раскрытия текста: `grid-template-rows:0fr→1fr` с transition ~.45s ease на обёртке + внутренний слой `overflow:hidden` с лёгким сдвигом `translateY(-8px)→0`; вертикальные отступы — марджинами содержимого (не паддингом обёртки, иначе высота не схлопнется), боковые — `padding:0 22px` на внутреннем слое; `prefers-reduced-motion` — без анимации. Без opacity (канон).
- **2026-07-08** — **Маркированные списки (ГЛОБАЛЬНО): маркер — круглая точка-булит, строки плотные.** Заказчик: «не тире, а красивые точки; расстояние между пунктами меньше». Тире «—» и штрихи-линии в маркерах СНЯТЫ везде. Канон-маркер: `::before` круг **5px** (на тёмных крупных списках 6px), `border-radius:50%`, `top:.56em`, цвет — золото `--gold-l` (на светлом в модале — navy); `padding-left:18–20px`. Межстрочный зазор пунктов ужат вдвое: 3–7px (было 6–14px). Затронуты: `.atm-ex__list`, `.atm-prep__list`, `.modal__list`, `.aor-list`, `.forwhom__list`; `.kit__list` с галочками ✓ остаётся как есть. Прежняя запись про «золотой штрих-маркер» в списках — отменена этой.
- **2026-07-08** — **ПРАВИЛО: вертикальный ритм маркированных списков (все булиты, глобально).** Принцип внутреннего и внешнего: (1) **внутри многострочного пункта строки плотнее, чем зазор между пунктами** — `line-height:1.3` у li (внутренний зазор ~4–5px), межпунктовый margin 6–8px → пункт читается единым блоком, пункты отделены друг от друга; внутреннее расстояние НИКОГДА не больше внешнего; (2) **список плотно прижат к своему заголовку** — отступ списка от заголовка/абзаца-ввода ~10px, без больших разрывов (список — продолжение заголовка); (3) сами списки компактные — межпунктовый зазор 6–8px, не 12–14. Точка-маркер при line-height 1.3 стоит на `top:.42em`. Применено ко всем спискам: `.atm-ex__list`, `.atm-prep__list`, `.modal__list`, `.aor-list`, `.forwhom__list`, `.kit__list` (✓-чек-лист — те же зазоры). Любой новый список верстается по этому правилу.
- **2026-07-08** — **Крупные курсивные строки-высказывания (Corsiva) — `line-height:.9` + цвет ЧЁРНЫЙ `--ink` на светлом.** Заказчик: у центрированных/курсивных надписей уменьшить межстрочное «везде», эталон — строка «Выделяйте время на отдых…» (`.aor-band__line`, `line-height:.9` = «90%»); и «дальше всё-таки делаем чёрного цвета». (1) **Межстрочный `.9`** для всех крупных Corsiva-высказываний лонгрида «Искусство отдыха»: `.aor-band__line` (уже был), `.aor-line`, `.aor-thought__title`, `.aor-thought__accent`, `.aor-question__not`, `.aor-question__yes`, `.aor-final__strong` (на navy — тоже поджат). Corsiva при `.9` не слипается (глифы связные), проверено на эталоне. (2) **Цвет — `--ink` (чёрный) вместо `--navy`** на светлых сценах: `.aor-band__line`, `.aor-line`, `.aor-thought__title`, `.aor-thought__accent`, `.aor-question__yes` (`__not` уже был ink). На тёмных сценах — как были (`.sec--navy .aor-line/__title` — золото `--gold-l`, `__accent` и `.aor-final__strong` — белый). Правило: **крупные курсивные строки-высказывания = чёрные на светлом, плотный межстрочный `.9`**; navy для них больше не используем.
- **2026-07-08** — **Карточки уровней отдыха («Сколько времени нужно», `.aor-tier`): иконка и заголовок «N ночей» — В ОДНУ СТРОКУ.** Заказчик: «иконка рядом с заголовком». Обёртка `.aor-tier__head{ display:flex; align-items:center; gap:14px }`, иконка 40px (`flex:0 0 40px`, без нижнего отступа) слева от числа `.aor-tier__n`. Тот же приём, что `.aor-card__head` — единый паттерн «иконка + заголовок в линию» для шапок карточек.
- **2026-07-08** — **Иконки — Phosphor Icons, начертание Fill (сменили Material Symbols).** Заказчик забраковал Material («отвратительные», разнобой, «дыра»-вихрь). Все концептуальные иконки сайта — из набора Phosphor (https://phosphoricons.com), вес **Fill** (сплошной силуэт — по духу прежнего правила «залитые»), инлайн-SVG `viewBox="0 0 256 256"`, `fill="currentColor"`, размер задаёт контейнер. Источник — пакет `@phosphor-icons/core` (devDependency), файлы `assets/fill/<имя>-fill.svg`; скрипт генерации `aor-icons.ts` — в скратчпаде сессии, при добавлении иконки можно просто скопировать path из пакета. Замены смыслов, где точной иконки нет: свеча → `flame`, кольцо/предложение → `sketch-logo` (гранёный бриллиант), двойное сердце → `heart`, женщина/мужчина → `gender-female`/`gender-male`, гормоны: окситоцин → `hand-heart`, дофамин → `sparkle`, серотонин → `flower-lotus`; ночи (сколько нужно отдыхать) → `moon` / `moon-stars` / `shooting-star`. Затронуто: `src/data/aor-icons.ts` (все 27), лента и карточки «Атмосферы», блок «Атмосфера для двоих» на главной, иконки-настроения финала «Дома». Material Symbols больше не берём; дубли одной иконки под разными смыслами — не допускаем.
- **2026-07-08** — **«Искусство отдыха»: единая типографическая шкала лонгрида (заказчик: «все разного размера, навести порядок, задать правила один раз»).** Было 7 кеглей наборного (14/15/16/17/18/19/20) и 3 размера карточного заголовка (17/18/21) — сведено к шкале: (1) **наборный текст сцены = Lora 500 18/1.5** (`.aor-p`, `.aor-qa p`, `.aor-thought p`, `.aor-question p`, `.aor-band__note`, `.aor-final__text`, `.aor-rhythm p`, `.aor-atm div`, `.aor-callout__text`, `.aor-ritual li`, `.aor-list li` — у списков lh 1.3 по канону списков); (2) **текст внутри карточек/плашек и вторичные заметки = 16/1.5** (`.aor-plate__x`, `.aor-card p`, `.aor-helper__*` (были 14–15), `.aor-tip`, `.aor-p--sm`, внутренности `.aor-tier`); (3) **подзаголовок сцены = один кегль**: `.aor-sub` = `.aor-h3` = Lora 600 clamp(21,2.2vw,26)/1.25; (4) **заголовок в карточке/плашке = Lora 600 18/1.35** (`.aor-plate__t`, `.aor-callout__strong` 21→18, `.aor-card__strong` 17→18). Кегли 14/15/17/19/21 в лонгриде ЗАПРЕЩЕНЫ. Hero-лид остаётся 20/1.4 (стиль hero).
- **2026-07-08** — **«Искусство отдыха»: вертикальный ритм заголовков — правила в CSS, инлайн-отступы сняты.** `.aor-h3{ margin:56px 0 12px }` — после наборного текста перед заголовком ВСЕГДА 56px (был инлайн-зоопарк 48/56/64/72); первый заголовок в колонке `.aor-duo`/`.aor-split` и внутри карточек — `margin-top:0` (воздух даёт контейнер, у грид-колонок марджины не схлопываются!); сетка/плашки сразу ПОСЛЕ заголовка прижаты: `.aor-h3 + .aor-plates/.aor-duo/.aor-tiles{ margin-top:20px }`, `.aor-helpers`/`.aor-atm` — тоже 20. Правило: отступы заголовков задаются ТОЛЬКО этими правилами, инлайн-margin на заголовках не пишем.
- **2026-07-08** — **Уточнение шкалы: ЛЮБАЯ жирная строка-ярлык внутри карточки/плашки = Lora 600 18/1.35, без исключений.** Заказчик заметил разнобой (барьеры 18 vs подпись гормона 16): `.aor-helper__tag`, `.aor-tier__lt`, `.aor-tier__plate p` подняты 16→18. В карточке ровно два кегля: жирный ярлык/заголовок 18/600, наборный текст 16/500.
- **2026-07-08** — **«Атмосфера»: плашки праздников И поводов раскрываются в КАРТОЧКУ-РАСКРЫТИЕ (фото + короткий текст) — как раздел «Досуг» на старом сайте (прямое требование заказчицы).** Голый лайтбокс «только фото» на сезонных карточках заменён; принцип один для «Атмосферы сезона» (8 шт.) и «Для вашей истории» (4 повода). Компонент `.atm-win`: тёмная подложка `rgba(20,15,14,.9)` (альфа запечена, не CSS opacity), карточка `--canvas` `min(1020px,94vw)`, грид «фото 1.35fr | текст 1fr», прямые углы, теней нет; крестик — белый на подложке (вне карточки, как в лайтбоксе). Навигация — НА фото: тонкие белые шевроны ‹ › (hover — золото) + белые точки внизу кадра (активная — золото); при одном кадре навигация скрыта. Текст: заголовок-связка Lora 700 21px ink, абзацы 16/1.5 ink. **Контент раскрытия лежит в HTML** (скрытый `div id="win-…"` внутри карточки, JS клонирует innerHTML) — текст индексируется поисковиками. Карточки-поводы получили галереи (обложка = первый кадр; фото подобраны из клиентских, заказчица обещала дать свои под каждый повод — заменить), hover-zoom 1.04 и белую подчёркнутую «Смотреть фото» в navy-плашке (`.detail-card__more`). Эталон предзагрузки кадров прежний (обложки в простое, галерея при наведении).
- **2026-07-08** — **«Атмосфера сезона»: SEO-фразы («Новый год за городом • Романтические выходные зимой»…) ВЕРНУЛИСЬ — внизу карточки-раскрытия.** Заказчица: фразы дописаны в её тексте ниже специально для поисковиков, «их надо не забыть добавить». На плашке их по-прежнему НЕТ (решение «строка тегов убрана из карточек» остаётся в силе) — они лежат внизу раскрытой карточки `.atm-win__tags` (Lora 600 14px, navy, прижаты к низу `margin-top:auto`) и присутствуют в статическом HTML. При правке текстов праздников фразы не терять.
- **2026-07-08** — **«Атмосфера»: hero = тёмная сцена `--ink`, текст слева, ВИДЕО заказчицы «по кругу» справа (докс ч.1, папка «снова что-то»).** Фоновое фото 13.4 со скримом заменено: `.atm-hero__grid` (текст `minmax(340px,42%)` | видео 1fr, `min-height:82vh`), видео `object-fit:cover`, зациклено (`autoplay muted loop playsinline`), правый край — full-bleed до края экрана (`margin-right:-32px` + `overflow:hidden` на секции). Исходник 15МБ ужат ffmpeg → `public/videos/atmosphere-hero.mp4` (1600px, crf25, без звука, ~4МБ) + постер-jpg. Тексты hero — дословно из докса ч.1 («Мы заранее все подготавливаем к Вашему приезду»); **кнопка hero — «Написать в ВК» → https://vk.me/park_sever** (прямое указание заказчицы: «кроме ВК ничего реально не работает»). ≤900px: колонка, видео 16/9 под текстом.
- **2026-07-08** — **«Атмосфера», поводы «Для вашей истории»: тексты-раскрытия и фото — из докса ч.1, плейсхолдеры сняты.** Заказчица передала папку «снова что-то»: докс ч.1 + фото 1.x–4.x под каждый повод (1.x предложение, 2.x ДР, 3.x годовщина, 4.x сюрприз) — оптимизированы в `src/assets/images/atmosphere/story/` (ПОДПАПКА: имена пересекаются с сезонными кадрами ч.2!). В раскрытии каждого повода — свой заголовок и SEO-текст из докса («Предложение в лесу / Когда не знаете где сделать предложение девушке…» и т.д.) — это те «подписи для поисковиков», о которых говорила заказчица; лежат в статическом HTML. Механика по ТЗ = старый park-sever.ru/entertainment (клик → фото листаются + текст) — наша карточка-раскрытие `.atm-win` ей соответствует. `extract-atmosphere.mjs` теперь парсит ОБА докса (руками не перепечатываем); лишний кадр 20260620 (кольцо в шкатулке) добавлен в галерею предложения.
- **2026-07-08** — **«Атмосфера сезона»: SEO-фразы видимы ПРЯМО НА ПЛАШКЕ (низ, `.season-card__tags`, Lora 600 13px navy) — заказчица настояла (третье напоминание), это отменяет запись «строка тегов убрана из карточек».** Дубль тех же фраз остаётся и внизу карточки-раскрытия. При правках карточек фразы не терять.
- **2026-07-08** — **«Атмосфера», hero: сцена — НЕ ink, а насыщенный синий `--navy`; левый край видео вплавлен в сцену градиентом.** Заказчик: фон плашки — «сильный синий наш» (или золотой — решили synий: тема «сцены» и золотая кнопка/лид на золоте бы пропали), граница видео должна быть плавной, «чтобы не было разделения-острова». Приём — как правый край карусели «Оазиса»: `.atm-hero__video::before` — `linear-gradient(90deg, var(--navy) 0%, rgba(15,42,71,.5) 14%, rgba(15,42,71,0) 44%)`, `pointer-events:none`; на ≤900px (видео под текстом) градиент разворачивается сверху вниз (180deg). Правило: **full-bleed медиа на цветной сцене вплавляем в неё градиентом с края, примыкающего к контенту.**
- **2026-07-08** — **«Атмосфера сезона»: на плашке — ПРЕВЬЮ (первое предложение текста), полное описание — только в карточке-раскрытии.** Заказчик: плашки показывали сразу всё описание — единый принцип с карточками-поводами: короткое превью на плашке, полный текст по клику. Превью берётся программно (`firstSentence()` от первого абзаца, руками не сочиняем — дословность). В плашке: заголовок-связка → превью → «Смотреть фото» → SEO-фразы.
- **2026-07-08** — **SEO-фразы карточек «Атмосферы сезона» — ЗОЛОТЫЕ (`--gold-l`), не navy.** Заказчик: «давай эти пометки золотым». И на плашке (`.season-card__tags`), и внизу карточки-раскрытия (`.atm-win__tags`) — единое золото сайта. Осознанное исключение из правила «на светлом текст ink»: это мета-строка-пометка, а не наборный текст.
- **2026-07-08** — **SEO-фразы «Атмосферы сезона» — ЧЁРНЫЕ (`--ink`).** Заказчик посмотрел золото и откатил: «лучше все черным». `.season-card__tags` и `.atm-win__tags` — сплошной `--ink` (по базовому правилу «на светлом — чёрный»). Запись про золотые пометки выше — отменена этой; navy и золото для этих строк не возвращать.
- **2026-07-08** — **SEO-фразы «Атмосферы сезона» — В САМОМ ВЕРХУ плашки (над заголовком), чёрные.** Заказчик: «булиты поставим сверху, прям сверху». Строка `.season-card__tags` (мелкая, Lora 600 13px, `--ink`, перечисление через «•») — первый элемент `.season-card__body`, над заголовком-связкой; порядок плашки: фразы → заголовок → превью → «Смотреть фото». В карточке-раскрытии фразы остаются ВНИЗУ (как в ТЗ «внизу перечислением»). Это осознанное исключение из правила «бирки над заголовком не используем» — по прямому указанию заказчика, только для этих строк.
- **2026-07-08** — **Карточки-поводы «Для вашей истории» — ЧИСТЫЙ кадр, текст ПОД фото (заказчик: «картинки не полностью, критично»).** Приём «текст на фото поверх navy-градиента» (.detail-card) на поводах СНЯТ: градиент с текстом закрывал нижнюю половину кадра (у «Предложения» — само кольцо). Теперь поводы = язык сезонных карточек (.season-card): портретный чистый кадр `.season-card__pic--tall{ aspect-ratio:3/4 }` (клиентские обложки 1.1/2.1/4.1 ровно 3:4 — видны целиком), белая иконка в левом верхнем углу остаётся, плашка под фото (у поводов — без боковых отступов, текст под фото как подписи «Наш мир»). Обложка «Годовщины» — портретный 3.3 вместо докс-обложки 3.1 (горизонтальный, надпись шарами резалась в портретном кадре; 3.1 остаётся в раскрытии) — если заказчица захочет 3.1 на плашку, кадр придётся делать горизонтальным.
- **2026-07-08** — **ОТКАТ: карточки-поводы «Для вашей истории» — снова текст НА фото (канонный .detail-card с navy-градиентом).** Заказчик вернул: «первый пункт верни, нужно было только фотки проработать». Запись выше про «чистый кадр, текст под фото» — отменена; из неё остаётся ТОЛЬКО проработка фото: обложка «Годовщины» = портретный кадр 3.3 (горизонтальный докс-кадр 3.1 — в раскрытии). Плашечный вид поводов: кадр 3/4, белая иконка в углу, текст на градиенте, белая подчёркнутая «Смотреть фото».
- **2026-07-08** — **«Атмосфера сезона»: кадр карточки — ПОРТРЕТНЫЙ 3/4 (был 4/3).** Екатерина: «фотки не целиком» — все 8 клиентских обложек праздников сняты 3:4 (портрет), в горизонтальном окне 4/3 cover резал ~44% кадра. Портретное окно совпадает с фото 1:1 — обрезки нет вовсе. Правило: **пропорцию окна карточки подбираем под фактические пропорции клиентских кадров** (как у поводов 3/4) — не наоборот.
- **2026-07-08** — **Карточка-раскрытие `.atm-win`: кадры — ЦЕЛИКОМ (`object-fit:contain`) на тёмной сцене `--ink`.** Екатерина: горизонтальные фото (например «Выходи за меня» у Предложения) резались cover-кадрированием — «или двигать фото, или целиком. Лучше второе». Окно медиа держит пропорцию (78%), кадр вписывается полностью, поля — тёмная сцена (читается как лайтбокс, не как «не влезло»). Правило: **в раскрытиях/лайтбоксах фото никогда не кадрируем — только contain**; cover остаётся для плашек-обложек, где пропорция окна подобрана под кадр.
- **2026-07-09** — **МОБИЛЬНЫЙ КАНОН (заказчик, по скринам главной): принцип «СНАЧАЛА ФОТО — ПОТОМ ТЕКСТ».** На мобиле текст НЕ кладём поверх кадра. Последовательность блока: чистое фото сверху (объект виден целиком), низ кадра мягко уходит градиентом в цвет сцены (фото «прослеживается»), текст идёт НИЖЕ на сплошной подложке. Применено: hero главной (`.hero__wrap{ padding-top:min(56vh,480px) }`, скрим с vh-стопами прозрачен до ~30vh → solid navy к 66vh) и «Дом среди леса» (`.house__content` static под слайдером, скрим — только фейд низа кадра в `--navy`). Любой новый блок «текст на фото» на мобиле перестраивается по этому принципу.
- **2026-07-09** — **Мобила: карточки/плашки — во всю ширину экрана, без белых полей.** «Искусство отдыха»: `.art-steps` на ≤520px — full-bleed (`margin:0 -32px`, gap 14px). «Оазис»: бумажный лист растянут почти до краёв (`margin:0 -28px`, внутренние отступы 36/26). Негативные поля = паддингу `.wrap` (32px) — горизонтального скролла не возникает.
- **2026-07-09** — **Мобильная типографика: `.h2` минимум 36px** (Corsiva на 30px читалась одним кеглем с наборным); в карточках «Искусства отдыха» на мобиле заголовок 24px против текста 16px. Лого шапки `.nav__logo` — ВСЕГДА одной строкой (`white-space:nowrap`, на ≤640px кегль 24px). Типограф висячих предлогов — глобально в `SiteLayout.astro` (все страницы), не по-страничн­о.
- **2026-07-09** — **«Атмосфера для двоих» на главной: иконки-поводы = бегущая лента `.atm-marquee` (и на ПК, и на мобиле).** Статичный ряд и анимация «дыхания» на месте (`icon-breathe`) СНЯТЫ — движение даёт только лента. Реализация — переиспользование маркиза со вкладки «Атмосфера»: 6 копий группы (`aria-hidden` у копий >0), модификатор `.atm-marquee--icons` (иконки золото 52px, подписи белые, gap 64/44).
- **2026-07-09** — **«Почему стоит выбрать нас» — коллаж: миниатюры-«открывашки» КОЛОНКОЙ СЛЕВА, сцена справа; высота НЕ прыгает.** `.adv-carousel` — grid `180px | 1fr`; на ПК сцена фикс-высоты 520px (текст при переполнении скроллится внутри), на мобиле фикс: фото 280px + текст `min-height:188px`, миниатюры рядом 4 (2×2 на ≤520px). Правило: переключаемые панели с разным текстом ВСЕГДА имеют зарезервированную высоту — интерфейс не дёргается (ПК и мобила).
- **2026-07-09** — **Стрелки «→» в текстовых ссылках УБРАНЫ навсегда** («Читать в ВКонтакте», «Мы ВКонтакте», «Открыть нашу группу» — главная и /stories/). Ссылку выдаёт подчёркивание (канон 2026-07-08), декоративные стрелки в ссылках не используем. Новости на мобиле — по принципу «фото во всю ширину → текст под ним» (`.news-card` в одну колонку, кадр 16/10; на главной показываем 3 свежих, внутренней прокрутки нет).
- **2026-07-09** — **Страница «Правовая информация» (`/legal-information`, URL 1:1 со старым сайтом) + колонка «Документы» в подвале ВСЕХ страниц.** Документы старого сайта подняты: PDF скачаны с park-sever.ru в `public/files/` (Договор-оферта.pdf 16 стр., Правила-проживания.pdf 5 стр.). Страница — канон: узкая колонка 760px на левой линии, h1, документы = cream-карточки (иконка Phosphor `file-text` fill золотом `--gold-l` 40px + заголовок Lora 600 18 + «Открыть PDF» ink-underline), реквизиты = dl-строки «ярлык 600/18 — значение 500/18», примечание 16/1.55; никаких декоративных линий. Подвал стал **4 колонки** (`1.6fr 1fr 1fr 1.1fr`, gap 56): бренд · Разделы · Контакты · **Документы** (Правовая информация / Договор-оферта PDF / Правила проживания PDF); адаптив ≤1100px — 2 колонки, ≤640px — 1. Отдельной политики конфиденциальности на старом сайте НЕ было (только эти 2 PDF + реквизиты) — если заказчик захочет, документ нужно готовить с нуля. В меню (6 вкладок) страница НЕ добавляется — вход только из подвала.
- **2026-07-09** — **Крупные курсивные высказывания (Corsiva) — ВСЕГДА по центру и крупно, как эталон `.aor-band__line` («Выделяйте время на отдых…» во введении).** Заказчик: «такие тексты пишем по центру, по центру крупно и с межстрочным 90%». `.aor-line` приведён к эталону: `font-size:clamp(38px,4.6vw,60px); text-align:center; margin:44px auto 0; max-width:30ch; text-wrap:balance; line-height:.9`. Левое выравнивание для таких строк-высказываний не используем; межстрочный `.9` и чёрный на светлом — по записи 2026-07-08.
- **2026-07-09** — **Вкладки-«странички» (лонгриды «Искусство отдыха», «Наш мир»): клик по вкладке скроллит К ЛИНИИ КОНТЕНТА, не в обложку и не оставляет внизу.** Заказчик: «обложку я уже видел — хочу сразу на линии основного содержания». Цель прокрутки = верх активной панели минус высота липкого меню-бара минус 56px шапки: `panel.offsetTop - bar.offsetHeight - 56` (offsetTop панели стабилен — sticky остаётся в потоке; сам `bar.offsetTop` использовать НЕЛЬЗЯ: у прилипшего бара он равен текущему скроллу — страница «не двигается»). Прыжок мгновенный, без smooth.
- **2026-07-09** — **«Искусство отдыха»: ОТКАЗ ОТ ПЛАШЕК — спокойный наборный текст; плашки только осознанно, для перечислений в 3+ колонки.** Заказчик: «глобально нужно избавиться от этих плашек… нужен спокойный наборный текст; использовать только очень осознанно, когда в три колонки идёт перечисление». (1) Сняты все одиночные плашки-выделения: navy-callout («Отдых — физиологическая необходимость…», «Дорога — часть отдыха», «Забота о себе»), frost-карточки («Маленький эксперимент», «Интересный факт», «Если усталость физическая/эмоциональная», «Запомните главное») и фон плашки ритуала — всё стало обычным наборным (`.aor-p` 18/1.5, заголовок `.aor-h3`, вывод — новый класс `.aor-strong` Lora 600 18/1.35 без фона). Классы `.aor-callout*`/`.aor-card` (фон) удалены из CSS; `.aor-card__head` (иконка+заголовок в линию) остаётся. (2) МНОГОКОЛОНОЧНЫЕ перечисления остаются плашками: `.aor-plates` (3–5 колонок), `.aor-helpers` (гормоны, 3), `.aor-tiers` (уровни отдыха, 3). (3) У «Маленького эксперимента» иконка-колба снята — стоит цветок (Phosphor `flower` fill, текст про дарение цветов). (4) **Примечание при кнопке** («Описание дома…») — новый класс `.aor-note`: 14/1.5, стоит ПОСЛЕ кнопки и прижато к ней (margin-top:10px); кегль 14 разрешён только для этого случая. Правило на будущее: новый акцент в лонгриде — сначала наборным текстом, плашка — только для перечисления в несколько колонок.
- **2026-08-24** — **Главная: блоки «Оазис» и «Искусство отдыха» переведены на инлайн-контент в `index.astro`** (по правкам заказчика в docx «24.08») — больше не читаются из `content/pages/home.json` (правило CLAUDE.md «по мере переработки блока переносим контент инлайн»). JSON-записи для этих секций остались в файле нетронутыми, но уже не влияют на страницу — не редактировать их ожидая эффекта на сайте.
- **2026-08-24** — **Новый блок «Что входит в ваш отдых» (главная, между «Оазис» и «Искусство отдыха»).** Узкая тёмно-синяя сцена (`.sec--navy`, обычный `.sec` паддинг — не на весь экран): заголовок золотой (`--gold-l`, по образцу заголовка «Атмосферы»), ниже ряд из 7 плашек `.aor-plate`/`.aor-plates--7` (переиспользован карточный язык лонгрида «Искусство отдыха» — светлая плашка `--frost` с тёмным текстом на тёмной сцене, без иконок), адаптив 7→4→2→1 колонка. Под плашками — обычный наборный блок (не плашка): подзаголовок `.aor-h3`, жирный ярлык-связка `.aor-strong` («Гастрономический сервис — единственная платная категория»), абзац `.aor-p`, список `.aor-list` (золотой булит на navy). Кнопки/иконок нет — решение принято без уточнения у заказчика по аналогии с «Оазисом», как минимально рискованный вариант; если заказчик захочет иконки на плашках — брать из `src/data/aor-icons.ts` (Phosphor Fill), не изобретать новый набор.
- **2026-08-24** — **«Искусство отдыха» на главной: 6 плашек → 4, десктоп в один ряд (`repeat(4,1fr)`), на мобиле — горизонтальный свайп вместо вертикального стека.** Заказчик прямо попросил: «в мобильной версии лучше пусть в сторону смахиваются, листаются, чтобы не была длинная главная страница». Это ОТМЕНЯЕТ на этом конкретном блоке мобильное правило 2026-07-09 «карточки full-bleed без зазоров, одна над другой» — вместо `grid-template-columns:1fr` на ≤520px теперь `display:flex; overflow-x:auto; scroll-snap-type:x mandatory`, карточка `flex:0 0 78%` (следующая выглядывает), gap 14px, поля 32px по бокам ленты. Кнопка «Узнать как работает отдых» (→ `/art-of-rest`) — центрированная, под лентой карточек, класс `.art-cta`. Остальные mobile-swipe-блоки (карусели «Оазиса»/«Атмосферы») это правило не трогает — они уже были на драг/скролле.
- **2026-08-25** — **`/services/`: заполнены гриль-меню, супы, шеф-повар из присланного pptx.** Заказчица прислала презентацию с 4 гриль-сетами (Северный гриль 7500₽, Вечер у костра 5000₽, Огонь и дым 6000₽, Вечер на террасе 8500₽ — состав и цены из файла дословно) и 3 супами (гороховый 3500₽, борщ 3500₽, куриный бульон 2500₽). Найдены и НЕ перенесены на сайт две нестыковки в исходнике: (1) у «Борща» и «Куриного бульона» было скопировано описание горохового супа («суп с копчёностями») — явная опечатка при копировании, оставили только общую часть (готовится заранее, объём 2–2,5 л); (2) услуга «Шеф-повар для мангала» в файле фигурирует с двумя разными ценами на разных слайдах (3500₽ и 3000₽/вечер) — цену на сайт не поставили вообще («Уточняем цену» на карточке, только описание в модале), дожидаемся от заказчицы, какая верная. Правило на будущее: **при переносе присланных заказчицей текстов на сайт — сверять цифры/повторы между слайдами/пунктами, копипаст-ошибки не размножать молча, конфликтующие данные не разрешать угадыванием**.
- **2026-08-25** — **Новая страница `/services/` («Дополнительные услуги») — каталог допуслуг, не в главном меню.** Заказчица прислала перечень из 7 категорий (гриль-меню, супы, шеф-повар для мангала, шары, торт, сервировка, индивидуальное оформление) с ценами по части из них — попросила отдельную страницу (не внутри «Атмосферы»), раскладку «в строчку, как Искусство отдыха». Решения: (1) **Не добавлена в шапку** — меню сайта зафиксировано на 6 вкладках (CLAUDE.md), страница открывается по кнопке «Подробнее» из блока «Что входит в ваш отдых» на главной (кнопка раньше вела на `/atmosphere/` — переключена на `/services/`, т.к. это точнее по смыслу) и из подвала — по образцу `/legal-information`. (2) **Фото нет вообще ни по одной категории** (заказчица ещё не прислала, из ВК/ТГ взять не вышло — см. запись про ТГ ниже) — карточки временно с плейсхолдером: крупный курсивный номер (`--gold-l`) в рамке `--hair-gold` вместо фото, структура (4+3 колонки, свайп на мобиле ≤520px) готова принять реальные фото в `<Image>`, когда придут — тогда это станет один в один art-step с главной. (3) **Клик по карточке открывает `.modal`** (переиспользован ровно тот же компонент/JS, что «Полная комплектация дома» на `/accommodation/house-1` — обобщили механику под несколько модалов через `data-modal-open="<id>"` вместо одного хардкод-id). (4) **Честно отмечены пробелы в контенте**, не выдуманы: гриль-меню/супы/шеф-повар/индивидуальное оформление — «Скоро добавим» (файл с текстом от заказчицы не дошёл до чата); сервировка — только названия+цены без состава (просила писать «что входит», а не «для кого», но сам состав не прислала) с пометкой «Состав уточняется». Шары и торт — данные были полные, добавлены как есть.
- **2026-08-25** — **«Что входит в ваш отдых»: кнопка «Подробнее» в блоке «Дополнительные услуги по желанию» → на `/atmosphere/`.** Заказчица попросила добавить переход на вкладку «Атмосфера для двоих» (там подробно про романтические/праздничные услуги). Добавлена в обе версии блока: десктоп (`.included-extra`, прямо на navy-сцене) — `.btn--gold` (контраст на тёмном, тот же приём, что у других CTA на navy); мобильная карточка (светлый `--frost` фон) — `.btn--navy` (тёмная кнопка на светлой плашке, как «Проложить маршрут» у «Оазиса»).
- **2026-08-25** — **«Что входит в ваш отдых»: на мобиле — свайп-карточки вместо длинного списка, десктоп/планшет не тронуты.** Заказчица: на телефоне не хочется долго листать вниз, лучше «плашками» и в сторону — тот же приём, что у «Искусство отдыха» (peek следующей карточки, scroll-snap). Группировка — по исходному порядку письма, не по длине текста (это отдельная логика от десктопной раскладки 3+4 выше): карточка 1 — три пункта (Инженерия, SPA, Завтрак), карточка 2 — два (Всё для огня, Отдых без доплат), карточка 3 — два (Познакомиться с оленеводством, Четвероногие терапевты), карточка 4 — блок «Дополнительные услуги по желанию» целиком. Реализация — оба варианта верстаются в разметке одновременно (`.included-desktop` / `.included-mobile`), переключаются `display:none`/`block` на ≤520px (тот же брейкпоинт, что у свайпа «Искусство отдыха»), а не пересобираются через JS/matchMedia — простой и надёжный способ для SSG-страницы без гидратации. Карточки на мобиле — **светлые** (`--frost` фон, `--ink` текст) в отличие от десктопной версии БЕЗ плашек: на мобиле это осознанно другая по духу задача (дискретные экраны для свайпа нужны читаемыми как отдельные карточки), а не то же решение, что раскритиковали на десктопе. Контент карточек не дублируется руками — деструктурируется из тех же `includedItemsWide`/`includedItemsNarrow` массивов, чтобы тексты не разъезжались при правках.
- **2026-08-25** — **«Что входит в ваш отдых»: два ряда, подобранные под длину текста, вместо единой сетки 7/3.** Единая 3-колоночная сетка (7 пунктов → 3 ряда: 3+3+1) оставляла последний ряд «сиротой» — 1 карточка на всю ширину, заметная пустая площадь справа, заказчица отметила, что выглядит обрезанно. Решение — сгруппировать пункты по длине текста в ДВА отдельных ряда-сетки: **ряд 1 (3 широкие колонки)** — три САМЫХ длинных описания (Инженерия комфорта, SPA-ритуал, Четвероногие терапевты); **ряд 2 (4 узкие колонки)** — четыре покороче (Завтрак, Всё для огня, Отдых без доплат, Познакомиться с оленеводством). 3+4=7 — оба ряда заполнены на всю ширину без остатка, короткий текст не страдает в узких колонках, длинный не дробится в широких. Реализация — `.included-items--3`/`.included-items--4` (два отдельных grid-контейнера, не один со смешанными span), контент явно переупорядочен в `index.astro` (`includedItemsWide`/`includedItemsNarrow`) под эту раскладку — это ИЗМЕНЯЕТ порядок показа пунктов относительно исходного письма заказчика (было 1-2-3-4-5-6-7 по порядку, стало сгруппировано по длине текста); если контент блока будут редактировать дальше — учитывать, что порядок теперь визуально осмысленный (не хронологический из письма). Правило на будущее: **при N пунктах в адаптивной сетке проверять частное на остаток** — если единственный вариант колонок оставляет мелкий хвост в последнем ряду, искать разбивку на два ряда разной ширины, которая делится без остатка (напр. 7 = 3+4, 5 = 2+3), а не подгонять один универсальный column-count.
- **2026-08-25** — **«Что входит в ваш отдых»: финальная раскладка — 3 колонки на десктопе с вертикальными линиями-разделителями (не 7).** После снятия плашек (см. запись ниже) заказчица попросила «сетку, которая растягивается горизонтально» — добавили тонкие вертикальные линии `rgba(255,255,255,.18)` между колонками (реюз мотива хейрлайнов сайта, напр. `.foot__rule`), весь блок стал читаться как единая сетка на всю ширину. Следующим шагом заказчица уточнила: 7 узких колонок дробили заголовки/текст по одному слову в строке («Познакомиться / с оленеводством») — свели к **3 колонкам**, 7 пунктов текут в 3 ряда (3+3+1), `align-items:stretch`, чтобы линии-разделители шли на всю высоту ряда. Адаптив: ≤900px — 2 колонки, ≤480px — 1 колонка без линий. Ловушка специфичности: сброс `border-left` на самом узком брейкпоинте нужно писать той же специфичностью, что и `:nth-child(3n+1)/:nth-child(2n+1)` из брейкпоинта выше — иначе более специфичный селектор из широкого брейкпоинта продолжает выигрывать на узком, даже если его правило идёт раньше по файлу.
- **2026-08-25** — **«Что входит в ваш отдых»: плашки-коробки убраны, остались 7 колонок наборного текста с золотым штрихом-разделителем.** Заказчица посмотрела на десктопе: `.aor-plate` (фон `--frost`, растянутый по высоте до самой длинной карточки) при разной длине текста в колонках читался как «где-то пустая коробка снизу» — grid по умолчанию тянет все ячейки ряда на высоту самой высокой. Решение — вариант «вообще без плашек», который заказчица сама предложила: `.included-item` без фона/паддинга (просто текст на сцене `--navy`), разделитель между колонками — тонкий золотой штрих 28×2px над заголовком (`.included-item__rule`, тот же мотив, что булит-маркер списков сайта — переиспользуем язык, не изобретаем новый). Без видимого контейнера разная длина текста не читается как «недозаполнено» — это просто колонки разной высоты, как в редакционной вёрстке. `.aor-plates--7` (модификатор с 7 колонками) удалён из CSS как более не используемый — не оставлять мёртвые классы.
- **2026-08-25** — **Найден и исправлен системный баг full-bleed: `.wrap--flush` внутри flex-родителя не дотягивался до правого края экрана.** Заказчица заметила на карусели «Атмосферы для двоих»: при скролле фото обрезались «по контейнеру», а не уходили за экран (в «Оазисе» с виду тот же приём работал нормально). Причина — `.wrap--flush{ margin-left:0 }` полагался на то, что унаследованный из `.wrap` `margin-right:auto` в обычном блочном потоке сам гасится до 0 (когда `width:auto` и `max-width:none`, по спеке блочной модели). Но `#atmosphere{ display:flex; flex-direction:column }` (одноэкранный блок) делает `.wrap--flush` флекс-айтемом — а на кросс-оси флекс-контейнера `margin:auto` НЕ гасится в 0, а поглощает свободное место, из-за чего обёртка сжималась (замерено Playwright: 320px вместо 390px на экране 390px) и правый bleed внутренней карусели (`margin-right:-32px`) останавливался на 70px раньше настоящего края. Другие «одноэкранные» flex-секции (`#our-world`) бага не показали (там `.wrap--flush` не участвует в full-bleed кросс-оси так же), но потенциально уязвимы к тому же классу проблемы. **Фикс — на уровне самого класса**, не точечно: `.wrap--flush{ margin-right:0; width:100% }` (было только `margin-left:0`) — работает одинаково в любом родителе (блок/флекс/грид), не полагаясь на неявное поведение auto-margin. Правило на будущее: **у full-bleed классов margin-right всегда задавать явным `0`, никогда не полагаться на auto→0 в блочном потоке** — это ломается в любом flex/grid-контейнере.
- **2026-08-25** — **Cookie-баннер + Политика конфиденциальности в подвале (заказчик прислал готовый PDF).** (1) PDF лёг в `public/files/Политика-конфиденциальности.pdf` (по образцу двух уже имевшихся документов), добавлен третьей карточкой в `.legal-docs` на `/legal-information` (сетка `repeat(2,1fr)` → `repeat(3,1fr)`, брейкпоинт под 2 колонки сдвинут на 900px) и третьей ссылкой в колонку «Документы» подвала — подвал задублирован по всем 7 страницам (общего Footer-компонента сайт не использует, есть только неиспользуемый легаси `src/components/Footer.astro` от старой JSON-модели — трогать не стал). (2) **Cookie-баннер** — новый элемент `SiteLayout.astro` (значит на ВСЕХ страницах разом): тонкая тёмно-синяя (`--navy`) полоса, зафиксированная снизу экрана (`position:fixed; z-index:150` — выше шапки/sticky-меню, ниже лайтбокса/модала 200+), текст белый сплошной, ссылка на политику — белая с подчёркиванием (канон-ссылка на тёмном), кнопка `.btn--gold` (единственный контрастный акцент на navy, тот же приём что у кнопки «Забронировать» в шапке). Согласие запоминается в `localStorage` (ключ `ps-cookie-consent`), баннер показывается один раз до принятия; чтение/запись обёрнуты в try/catch (приватный режим браузера может блокировать storage — тогда баннер просто показывается каждый раз, сайт не ломается). Текст — дословно по просьбе заказчика, кнопка — «Хорошо» (спокойный тон бренда, не «Принять всё» капсом).
- **2026-08-25** — **Мобильная доводка после живого ревью заказчицы (скриншоты с телефона).** (1) **«Искусство отдыха»** — свайп-карточки на мобиле теперь БЕЗ зазора (`gap:0`, было 14px) — соединены друг с другом вплотную, листаются одним фото-потоком (частично возвращает мобильный принцип 2026-07-09 «единый поток без зазоров», но сохраняя новую механику свайпа вместо вертикального стека). (2) **«Что входит в ваш отдых»** — курсив у пометки «(единственная категория услуг, которая оплачивается отдельно)» убран, обычным начертанием — заказчица прямо попросила без италика. (3) **Карусель «Оазиса» на мобиле** — левый золотой fade-градиент (`.oasis-carousel--wide::before`) отключён: он был не нужен (карусель — первый элемент, слева нечему «доедать» край) и визуально подрезал первую фотографию. Карточки укрупнены (`flex-basis:90%`, было 80%) и подтянуты к плашке (убран верхний паддинг трека 10px→0), у самой плашки `.oasis-paper` увеличены верхний/нижний внутренние отступы (36/42px → 48/56px) — заголовок и кнопка «Проложить маршрут» раньше стояли впритык к краю бумаги («нет полей»). (4) **Карусель «Атмосферы для двоих» на мобиле** — карточки укрупнены до того же 82%, что у базовой `.oasis-card` (`.oasis-card--sm` раньше не имела мобильного оверрайда — из-за равной специфичности CSS-правил мелкий `clamp(220px,30%,340px)` перебивал крупный мобильный вид, на телефоне были видны два криво обрезанных фото вместо одного явного «читай — дальше есть ещё»). Правило на будущее: у любого нового `.oasis-card--*`-варианта карусели проверять, что мобильный размер задан явно — специфичность CSS-классов здесь не даёт унаследовать базовый мобильный оверрайд автоматически.
- **2026-08-24** — **Фото шага 01 «Искусство отдыха» заменено.** Заказчица прислала новое фото (пробка на выезде из города) внутри docx-вложения — Word сжал картинку до 357×268px при вставке в документ, для сайта это было слишком мелко (текущие кадры — от 2000px). Попросили оригинал файлом напрямую — прислала 1024×768, этого достаточно (карточки запрашивают максимум 800px) — поставлено в `src/assets/images/home/art/art-step-1.jpg`. На будущее: если контент присылают внутри docx, извлекать фото и проверять разрешение (`PIL Image.size`) ДО того, как ставить в вёрстку — Word пережимает вставленные картинки.
- **2026-08-29** — **`/services/`: «Сервировка» — первая категория каталога с реальными фото и полным составом.** Заказчица прислала pptx с сервировками (6 вариантов оформления стола) — из презентации извлечены дословный текст (списки «что входит», без строк «подходит для…» — заказчица явно просила их не переносить) и 6 фото обложек через `zipfile`/`python-pptx` (у 3 из 6 фото — поворот на уровне трансформации фигуры в PPT, `rot="5400000"` в XML `<p:pic>`, не запечённый в пикселях; сопоставлены по `r:embed`→slide `.rels` и повёрнуты `PIL Image.rotate(-90, expand=True)` перед тем, как класть в проект). В перечне заказчицы значилось 5 сервировок — в файле нашлась 6-я, «Мужская шалость: карты, сигареты и сливки» (3000₽), не упомянутая в голосовом сообщении — добавлена как есть (в файле она есть — не выдумана, но заказчицу стоит уведомить, что список расширился на пункт сверх её перечисления). Отдельно со слайдов 7–10 вытащено меню блюд по отдельности (`alacarte`, 10 позиций — фруктовая тарелка, блины, брускеты и т.д.) — в файле оно было отдельным разделом от сервировок, поэтому в модале «Сервировка» вынесено отдельным блоком под сервировками, а не смешано с ними. Карточка категории 06 на `/services/` (десктоп-сетка и мобильный свайп) теперь показывает первое фото («Бордовая роза») вместо плейсхолдера-номера — карточка стала один в один `art-step` с главной, ровно то расширение, под которое верстался плейсхолдер (см. запись 2026-08-25 про `/services/`); остальные категории без фото продолжают показывать номер, переключение идёт по наличию `photo:` у категории, без переделки структуры. Правило подтверждено ещё раз: **числа/пункты из присланных файлов сверять на полноту, а не только на противоречия** — в этот раз расхождение было не в цифрах, а в количестве пунктов (5 названо голосом, 6 в файле).
- **2026-08-29** — **«Искусство отдыха» (`/art-of-rest`): копирайт-плашка в hero, справа, с раскрытием по наведению.** Заказчица: «справа аккуратно слова "Авторская концепция. Все права защищены", при наведении — что копирование карается законом». Новый элемент `.aor-copyright` — абсолютно спозиционирован в правом нижнем углу hero-фото (`right:32px`, вровень с внутренним краем `.wrap`), белый текст 13px, без рамки/фона — просто подпись на фото, как принято у фотокредитов. По `hover`/`focus` (`tabindex="0"` — доступно с клавиатуры) раскрывается `.aor-copyright__tip` — переиспользован ТОЧНО тот же визуальный язык, что у `.aor-term__tip` (научные сноски на этой же странице): песочный фон `--sand`, золотая обводка `--hair-gold`, текст `--ink`. На мобиле (≤640px) плашка переезжает в верхний правый угол (`top:24px`) — внизу hero на телефоне мало места, там заголовок и лид-текст.
- **2026-08-29** — **«Атмосфера для двоих»: блоки «Отдых начинается…» и «Примеры работ» подняты выше, сразу после «Для вашей истории».** Заказчица явно попросила порядок: первый блок → эти два → остальное. Было: hero → «Для вашей истории» → «Атмосфера сезона» → «Отдых начинается…» → «Примеры работ» → «Каждая история...». Стало: hero → «Для вашей истории» → «Отдых начинается…» → «Примеры работ» → «Атмосфера сезона» → «Каждая история...». Чисто перестановка секций в разметке, контент и JS не трогали — `id`/`data-*` атрибуты не менялись, поэтому ссылки на якоря (`#prepared`, `#examples`, `#seasons`) не сломались.
- **2026-08-29** — **«Отдых начинается ещё до вашего приезда»: текст обновлён, перечень допуслуг синхронизирован с `/services/`.** Заказчица прислала новую версию текста (взамен старой из докса «ч.1», где в базовый комплект входили ещё мангальные принадлежности) — обновлено в `src/data/atmosphere.json` → `prepared.paragraphs`/`extras`: по умолчанию теперь — завтрак, свечи+базовая сервировка, вывески (с примерами надписей); мангал/гриль/супы/шары/торт/сервировка/индивидуальное оформление — единым списком «Дополнительно можно заказать» (те же 7 категорий, что и на `/services/` — теперь два места с одним и тем же перечнем, при правке одного сверять с другим). Под списком добавлена текстовая ссылка (без класса, авто ink/white+подчёркивание — канон для ссылок на цветных сценах) «Подробнее о дополнительных услугах» → `/services/`, отдельно от уже имевшейся кнопки `.btn--gold` «Обсудить ваш отдых» (та ведёт на форму `#story` — разные цели, оставлены обе). **Не сделано:** заказчица просит коллаж из фото вывесок («С днём рождения», «I love you», «я тебя люблю», «happy birthday») — реальных фото вывесок в проекте нет, коллаж не из чего собрать, текст добавлен, фото — ждём от заказчицы.
- **2026-08-29** — **`/services/`: «Супы» — вторая категория с реальным фото обложки.** Заказчица прислала фото (кастрюля супа на плите). Заведено как `photo: 'soup-1.jpg'` в `categoriesTop`, ровно тот же conditional-рендер `cat.photo`, что уже был у «Сервировки» — но десктопный верхний ряд (`categoriesTop`, 4 колонки) ДО этого не имел такого условия вообще (только нижний ряд `categoriesBottom` и мобильный свайп его поддерживали, т.к. до сих пор ни у одной категории верхнего ряда не было своего фото) — добавлен идентичный `cat.photo ? <art-step> : <плейсхолдер>` блок и туда. Правило на будущее: у `/services/` теперь ТРИ места, где рендерится карточка категории (`svc-steps--4`, `svc-steps--3`, мобильный `svc-swipe`) — при добавлении фото к любой категории верхнего ряда проверять, что все три места умеют его показать, а не только те два, что уже были обкатаны на «Сервировке».
- **2026-08-29** — **`/services/`: карточки+модалы ПОЛНОСТЬЮ заменены на вкладочную навигацию — как «Этап 1,2,3» на `/art-of-rest`.** Заказчица: «давай в другом формате... как у нас этапы с тобой... верхняя менюшка, и это будет навигация по разделам... чтобы не было модальных переходов, сразу человек открыл и всё видит». Переиспользован ТОЧНО тот же механизм странички, что на `/art-of-rest` и `/our-world/`: горизонтальная sticky-менюшка `.ow-menubar`/`.ow-menu` (те же классы, без модификаций) с 7 вкладками (`data-svc-tab`), под ней 7 панелей (`data-svc-panel`, скрыты все кроме активной через `hidden`), JS-переключатель — почти дословная копия скрипта `/art-of-rest` (hash в URL, скролл к линии контента за вычетом высоты меню-бара и шапки), только с своим неймспейсом `data-svc-*`, чтобы не конфликтовать, если скрипты когда-нибудь окажутся на одной странице. Первая вкладка по умолчанию — «01. Гриль-меню» (заказчица явно попросила именно такой порядок открытия). Карточки-плитки (`.svc-steps`, `.svc-card`, `.svc-swipe`) и все 7 `.modal` блоков — удалены целиком вместе с их JS и CSS (весь `[data-modal-open]`-механизм на этой странице был только ради этих карточек — убран без остатка). Контент каждой категории переехал из тесной модалки в полноширинный `.sec`: гриль-сеты — `.aor-duo` (2×2 сетка), супы — `.aor-split` (список слева, фото `soup-1.jpg` справа), сервировка — `.aor-split` НА КАЖДЫЙ вариант (текст+фото), остальные — простые `.aor-list`/`.aor-p`. Ничего нового не изобретали — везде переиспользованы существующие классы лонгрида `/art-of-rest` (`.aor-h3`, `.aor-p`, `.aor-list`, `.aor-duo`, `.aor-split`), поэтому дизайн страницы автоматически попал в канон без единой новой CSS-конструкции. Заодно найден и исправлен pre-existing баг: на этой странице никогда не было скрипта мобильного гамбургер-меню (`.nav__toggle`) — при переписывании файла добавлен тот же сниппет, что на всех остальных страницах сайта.
- **2026-08-29** — **«Искусство отдыха» (`/art-of-rest`): новая вкладка «Об эксперте» в конце строки «Этап 1…6».** Заказчица прислала легаси `.doc` (не `.docx`) с текстом психолога, с которым готовился раздел — стандартные инструменты (`python-docx`, LibreOffice headless `--convert-to`) не смогли открыть файл («source file could not be loaded» — похоже, файл специфично собран старым Word, LibreOffice его не парсит), помогла связка `antiword` (`apt-get install antiword`) + ручное декодирование вывода как `cp1251` (`file` показал `Code page: 1251`) — antiword сам вывод не перекодирует, отдаёт сырые байты исходной кодовой страницы. Проверено через `olefile`: в документе нет embedded-картинок (только текстовые OLE-стримы) — фото эксперта не прикладывалось, панель текстовая. Вкладка добавлена ПОСЛЕДНЕЙ в `.ow-menu` (после «Этап 6»), контент — инлайн-объект `expert` в фронтматтере (не в `art-of-rest.json`: та схема — только «этапы», раздел про эксперта другого рода), рендер — переиспользованы ровно те же типографические классы страницы: `.aor-h3`/`.aor-p`/`.aor-list`/`.aor-strong`/`.aor-sub`, закрывающая цитата — `.aor-thought` (тот же приём, что у финальной мысли каждого этапа). Правило на будущее: **если `.doc`/`.docx` не читается стандартными библиотеками и LibreOffice падает с «source file could not be loaded» — пробовать `antiword` (пакет в apt), но ОБЯЗАТЕЛЬНО перекодировать его вывод из кодовой страницы, указанной в `file <путь>` (обычно `cp1251` для русских документов из старого Word) — иначе получится нечитаемая кракозябра, которую легко принять за «файл не читается», хотя данные извлеклись верно.
